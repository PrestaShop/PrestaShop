const CommonPage = require('../commonPage');

module.exports = class PRODUCTS extends CommonPage {
  constructor(page) {
    super(page);

    this.pageTitle = 'Products •';
  }

  /*
  Methods
   */
};
