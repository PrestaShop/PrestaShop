const CommonPage = require('../commonPage');

module.exports = class ORDERS extends CommonPage {
  constructor(page) {
    super(page);

    this.pageTitle = 'Orders •';
  }

  /*
  Methods
   */
};
