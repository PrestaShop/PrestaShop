{
   "name": "prestashop-common",
   "version": "1.0.0",
   "scripts": {
       "install": "npm -C js/vendor install; npm -C admin-dev/themes/default install; npm -C tests/js install",
       "upgrade": "npm -C js/vendor update; npm -C admin-dev/themes/default update; npm -C tests/js update",
       "test": "cd tests/js && grunt test",
       "test-short": "cd tests/js && grunt test-short",
       "test-verbose": "cd tests/js && grunt --stack test-verbose"
   }
}
