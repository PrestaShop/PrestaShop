{# this is temporary, shop context and list of shops will be stored in presentation class, made available here #}

{% if isMultistoreUsed %}
<div style="width:100%; height: 40px; background-color:#d06868; padding:12px;">
<h2 style="color: white; font-size: 14px; text-align:center;">
  {% if currentShop %}
    {{ currentShop.name }}
  {% else %}
    {{ currentShopGroup.name }}
  {% endif %}
</h2>
</div>
{% endif %}
