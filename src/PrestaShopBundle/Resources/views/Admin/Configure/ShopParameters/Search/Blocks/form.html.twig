{#
 # For the full copyright and license information, please view the
 # docs/licenses/LICENSE.txt file that was distributed with this source code.
 #}
{% form_theme form '@PrestaShop/Admin/Configure/ShopParameters/Search/FormTheme/aliases.html.twig' '@PrestaShop/Admin/TwigTemplateForm/prestashop_ui_kit.html.twig' %}

{{ form_start(form) }}
  <div class="card">
    <h3 class="card-header">
      {{ 'Alias'|trans({}, 'Admin.International.Feature') }}
    </h3>

    <div class="card-body">
      <div class="form-wrapper">
        {{ form_errors(form) }}
        {% block form_alias_widget %}
          {{ form_widget(form) }}
        {% endblock %}
      </div>
    </div>

    <div class="card-footer">
      <div class="d-inline-flex">
        <a href="{{ path('admin_search_alias_index') }}" class="btn btn-outline-secondary">
          {{ 'Cancel'|trans({}, 'Admin.Actions') }}
        </a>
      </div>
      <div class="d-inline-flex float-right">
        <button class="btn btn-primary" id="save-button">{{ 'Save'|trans({}, 'Admin.Actions') }}</button>
      </div>
    </div>
  </div>
{{ form_end(form) }}
