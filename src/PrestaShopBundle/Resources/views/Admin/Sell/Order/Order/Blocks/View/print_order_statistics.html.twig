<div class="card">
  <div class="card-body">
    <div class="row">
      <div class="col-sm">
        <p>
          <strong>{{ 'Date'|trans({}, 'Admin.Global') }}</strong>
        </p>
        <strong>random date</strong>
      </div>
      <div class="col-sm">
        <p>
          <strong>{{ 'Total'|trans({}, 'Admin.Global') }}</strong>
        </p>
        <p>
          <span class="badge rounded badge-dark font-size-100">
            {{ orderForViewing.prices.totalAmountFormatted }}
          </span>
        </p>
      </div>
      <div class="col-sm">
        <p>
          <strong>
            {{ 'Message'|trans({}, 'Admin.Global') }}
          </strong>
        </p>
        <strong>
          {# todo: currently visible will always be 2 . However in another PR @see https://github.com/PrestaShop/PrestaShop/pull/16255 there is a new method to retrieve them all.          #}
          2 (todo - change me)
        </strong>
      </div>
      <div class="col-sm">
        <p>
          <strong>
            {{ 'Products'|trans({}, 'Admin.Global') }}
          </strong>
        </p>
        <strong>
          {{ orderForViewing.products.products|length }}
        </strong>
      </div>
    </div>
  </div>
</div>
