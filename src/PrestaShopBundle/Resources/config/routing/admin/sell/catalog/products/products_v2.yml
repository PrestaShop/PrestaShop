#@todo: think of better approach for product routes
admin_products_v2_images:
  path: /{productId}/images
  methods: POST
  defaults:
    _controller: PrestaShopBundle:Admin/Sell/Catalog/Product/Image:upload
  requirements:
    productId: \d+
  options:
    expose: true
