services:
  _defaults:
    public: true

  prestashop.adapter.security.employee_authentication_handler:
    class: PrestaShop\PrestaShop\Adapter\Security\EmployeeAuthenticationHandler
    arguments:
      - '@prestashop.adapter.legacy.context'
      - '@security.token_storage'
      - '@prestashop.security.admin.provider'
      - '@prestashop.static_cache.adapter'
      - '@request_stack'

  prestashop.adapter.security.employee_logout_listener:
    class: PrestaShop\PrestaShop\Adapter\Security\EmployeeLogoutListener
    arguments:
      - '@prestashop.adapter.legacy.context'
