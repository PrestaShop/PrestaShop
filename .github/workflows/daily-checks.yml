name: Cron Daily Checks
on:
    schedule:
        - cron:  '0 23 * * *'
jobs:
  security-checker:
    name: Composer Security Checker
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Local PHP Security Checker
        uses: docker://pplotka/local-php-security-checker-github-actions
