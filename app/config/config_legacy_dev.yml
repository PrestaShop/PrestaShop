imports:
  - { resource: config_legacy.yml }

doctrine:
  orm:
    metadata_cache_driver:
      type: pool
      pool: array
    query_cache_driver:
      type: pool
      pool: array_doctrine
    result_cache_driver:
      type: pool
      pool: array_doctrine
